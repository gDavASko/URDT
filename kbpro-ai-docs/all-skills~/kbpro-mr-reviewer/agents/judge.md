# Устаревший локальный профиль Judge

Этот файл не является запускаемым агентом. Для независимой оценки использовать только
зарегистрированного `judge` из `kbpro-subagent-team/agents/registry.yaml`; он не должен
выполнять существенную часть проверяемого анализа.
