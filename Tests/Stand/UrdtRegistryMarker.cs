using UnityEngine;

namespace KBP.URDT.Tests.Stand
{
    /// <summary>
    /// Trivial stable-trait marker used by the M3 registry tests to make an object
    /// match a <c>hasComponent</c> selector rule. Holds no state and does nothing.
    /// </summary>
    public sealed class UrdtRegistryMarker : MonoBehaviour
    {
    }
}
