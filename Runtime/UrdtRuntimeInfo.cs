namespace KBP.URDT
{
    /// <summary>
    /// Package-level constants of the Unity Runtime Debug Tool.
    /// The whole assembly compiles only when the URDT_ENABLED define is set.
    /// </summary>
    public static class UrdtRuntimeInfo
    {
        public const string PACKAGE_NAME = "DavASkoURDT";
        public const string VERSION = "0.1.0";
    }
}
